<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="myprojects.alzingrejm.com.songs4heaven_v2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MultiSearchActivityLaunch" />
        <!--suppress AndroidDomInspection -->
        <activity android:name=".MultiSearchActivityFragment" />
        <!--suppress AndroidDomInspection -->
        <activity android:name=".MultiSearchActivity" />
        <activity android:name=".PastorsSongs" />
        <activity android:name=".PastorsSongsDetails" />

        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Songs4HeavenActivity" />
        <activity android:name=".SongPagerActivity" />
        <activity android:name=".SearchResults" />
    </application>

</manifest>